server:
  port: 8081
spring:
  application:
    name: backendconfig-service
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/Alexandra-Navarro/Mingeso_pep3
          search-paths: "microservicios/config-data"
